package search

// This file was generated by the swagger tool.
// Editing this file might prove futile when you re-run the generate command

import (
	"errors"
	"net/url"
	golangswaggerpaths "path"
	"strings"

	"github.com/go-openapi/swag"
)

// GetCharactersCharacterIDSearchURL generates an URL for the get characters character id search operation
type GetCharactersCharacterIDSearchURL struct {
	CharacterID int32

	Categories []string
	Datasource *string
	Language   *string
	Search     string
	Strict     *bool
	Token      *string
	UserAgent  *string

	_basePath string
	// avoid unkeyed usage
	_ struct{}
}

// WithBasePath sets the base path for this url builder, only required when it's different from the
// base path specified in the swagger spec.
// When the value of the base path is an empty string
func (o *GetCharactersCharacterIDSearchURL) WithBasePath(bp string) *GetCharactersCharacterIDSearchURL {
	o.SetBasePath(bp)
	return o
}

// SetBasePath sets the base path for this url builder, only required when it's different from the
// base path specified in the swagger spec.
// When the value of the base path is an empty string
func (o *GetCharactersCharacterIDSearchURL) SetBasePath(bp string) {
	o._basePath = bp
}

// Build a url path and query string
func (o *GetCharactersCharacterIDSearchURL) Build() (*url.URL, error) {
	var result url.URL

	var _path = "/characters/{character_id}/search/"

	characterID := swag.FormatInt32(o.CharacterID)
	if characterID != "" {
		_path = strings.Replace(_path, "{character_id}", characterID, -1)
	} else {
		return nil, errors.New("CharacterID is required on GetCharactersCharacterIDSearchURL")
	}
	_basePath := o._basePath
	if _basePath == "" {
		_basePath = "/latest"
	}
	result.Path = golangswaggerpaths.Join(_basePath, _path)

	qs := make(url.Values)

	var categoriesIR []string
	for _, categoriesI := range o.Categories {
		categoriesIS := categoriesI
		if categoriesIS != "" {
			categoriesIR = append(categoriesIR, categoriesIS)
		}
	}

	categories := swag.JoinByFormat(categoriesIR, "")

	if len(categories) > 0 {
		qsv := categories[0]
		if qsv != "" {
			qs.Set("categories", qsv)
		}
	}

	var datasource string
	if o.Datasource != nil {
		datasource = *o.Datasource
	}
	if datasource != "" {
		qs.Set("datasource", datasource)
	}

	var language string
	if o.Language != nil {
		language = *o.Language
	}
	if language != "" {
		qs.Set("language", language)
	}

	search := o.Search
	if search != "" {
		qs.Set("search", search)
	}

	var strict string
	if o.Strict != nil {
		strict = swag.FormatBool(*o.Strict)
	}
	if strict != "" {
		qs.Set("strict", strict)
	}

	var token string
	if o.Token != nil {
		token = *o.Token
	}
	if token != "" {
		qs.Set("token", token)
	}

	var userAgent string
	if o.UserAgent != nil {
		userAgent = *o.UserAgent
	}
	if userAgent != "" {
		qs.Set("user_agent", userAgent)
	}

	result.RawQuery = qs.Encode()

	return &result, nil
}

// Must is a helper function to panic when the url builder returns an error
func (o *GetCharactersCharacterIDSearchURL) Must(u *url.URL, err error) *url.URL {
	if err != nil {
		panic(err)
	}
	if u == nil {
		panic("url can't be nil")
	}
	return u
}

// String returns the string representation of the path with query string
func (o *GetCharactersCharacterIDSearchURL) String() string {
	return o.Must(o.Build()).String()
}

// BuildFull builds a full url with scheme, host, path and query string
func (o *GetCharactersCharacterIDSearchURL) BuildFull(scheme, host string) (*url.URL, error) {
	if scheme == "" {
		return nil, errors.New("scheme is required for a full url on GetCharactersCharacterIDSearchURL")
	}
	if host == "" {
		return nil, errors.New("host is required for a full url on GetCharactersCharacterIDSearchURL")
	}

	base, err := o.Build()
	if err != nil {
		return nil, err
	}

	base.Scheme = scheme
	base.Host = host
	return base, nil
}

// StringFull returns the string representation of a complete url
func (o *GetCharactersCharacterIDSearchURL) StringFull(scheme, host string) string {
	return o.Must(o.BuildFull(scheme, host)).String()
}
